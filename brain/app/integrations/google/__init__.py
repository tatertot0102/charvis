"""Google integration (Phase 2A): read-only Calendar behind an OAuth flow.

The act side of any Google connector stays out of this phase (read-only, per MVP.md). Only the
OAuth flow, encrypted token storage, and Calendar read live here.
"""
