"""Phase 2D.3 query layer — deterministic time-range parsing, query planning, structured answers.

Everything here is provider-agnostic and truth-preserving: ranges are computed, answers are assembled
from real evidence before any prose is written, and the validator blocks fabricated or capability-
denying replies (Golden Rule #7).
"""
